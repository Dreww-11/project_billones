# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.

Here is a simple, student-friendly reflection for your project:

My Reflection on Full-Stack & React.js
What I’ve Learned
In our discussions, the biggest "lightbulb moment" was moving from basic HTML to Component-Based Architecture in React. I learned that instead of writing one long, messy file, I can build a website like a set of LEGO blocks—creating small, reusable pieces like a Navbar or a Profile Card. I’ve also grasped the concept of State (useState), which allows my apps to "remember" things and react to what a user does in real-time. Seeing how the Frontend (React) and Backend (PHP/MySQL) finally talk to each other made the "Full-stack" concept much clearer to me.

What I Want to Learn Further
I’m really excited to keep pushing forward. I want to move beyond the basics of State and learn Context API or Redux to handle data in much larger apps. I’m also very curious about Web Security—learning how to properly hide passwords and protect my database from common attacks. Finally, I want to learn the "DevOps" side of things: how to actually deploy my React projects to a live URL so I can share my work with the world.
